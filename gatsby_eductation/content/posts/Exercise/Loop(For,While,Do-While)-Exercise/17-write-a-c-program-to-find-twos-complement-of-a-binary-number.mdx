---
title: Write a C/C++/Java program to find two’s complement of a binary number.
category: Loop(For,While,Do-While)Index
tags: ['loop']
date: 2020-04-16
thumbnailText: Index
---

## C Implementation
### 17. Write a C program to find two’s complement of a binary number.
```c
//Write a C program to find two’s complement of a binary number.
//Using for loop
#include <stdio.h>
#define SIZE 8
int main()
{
    char b[SIZE + 1], onesComp[SIZE + 1], twosComp[SIZE + 1];
    int i, c=1;
    printf("Enter %d bit binary value: ", SIZE);
    gets(b);
    for(i=0; i<SIZE; i++)
    {
        if(b[i] == '1')
        {
            onesComp[i] = '0';
        }
        else if(b[i] == '0')
        {
            onesComp[i] = '1';
        }
    }
    onesComp[SIZE] = '\0';
    for(i=SIZE-1; i>=0; i--)
    {
        if(onesComp[i] == '1' && c == 1)
        {
            twosComp[i] = '0';
        }
        else if(onesComp[i] == '0' && c == 1)
        {
            twosComp[i] = '1';
            c = 0;
        }
        else
        {
            twosComp[i] = onesComp[i];
        }
    }
    twosComp[SIZE] = '\0';
    printf("Original binary = %s\n", b);
    printf("Ones complement = %s\n", onesComp);
    printf("Twos complement = %s\n", twosComp);
    return 0;
}
//Using while loop
#include<stdio.h>
void main()
{
    int i,a,sum=0;
    i=1;
    while(i<=3)
    {
        scanf("%d",&a);
        sum=sum+a;
        i++;
    }
    printf("%d",sum);
}
//Using do while loop
#include<stdio.h>
void main()
{
    int i=1,a,sum=0;
    do
    {
        scanf("%d",&a);
        sum=sum+a;
        i++;
    }
    while(i<=3);
    printf("%d",sum);
}
```
### Output
```
input 3 4 5
output 12
```

## C++ Implementation
### 17. Write a C++ program to find two’s complement of a binary number.
```c
//Write a C++ program to find two’s complement of a binary number.
#include<iostream>
using namespace std;
int main()
{
    cout<<"hello world";
    return 0;
}
//Using for loop
#include<iostream>
using namespace std;
int main()
{
    int i,a,sum=0;
    for(i=1;i<=3;i++)
    {
        scanf("%d",&a);
        sum=sum+a;
    }
    cout << sum;
}
//Using while loop
#include<iostream>
using namespace std;
int main()
{
    int i,a,sum=0;
    i=1;
    while(i<=3)
    {
        scanf("%d",&a);
        sum=sum+a;
        i++;
    }
    cout << sum;
}
//Using do while loop
#include<iostream>
using namespace std;
int main()
{
    int i=1,a,sum=0;
    do
    {
        scanf("%d",&a);
        sum=sum+a;
        i++;
    }
    while(i<=3);
    cout << sum;
}
```
### Output
```
input 3 4 5
output 12
```

## Java Implementation
### 5. Write a Java program to find two’s complement of a binary number.
```js
//Write a Java program to find two’s complement of a binary number.
//Using for loop
import java.util.Scanner;
public class Funycode {
    public static void main(String[] args) {
        Scanner z = new Scanner(System.in);
        int i,a,sum=0;
        i=1;
        while(i<=3)
        {
            a = z.nextInt();
            sum=sum+a;
            i++;
        }
        System.out.println(sum);
    }
}
//Using while loop
import java.util.Scanner;
public class Funycode {
    public static void main(String[] args) {
        Scanner z = new Scanner(System.in);
        int i,a,sum=0;
        for(i=1;i<=3;i++)
        {
            a = z.nextInt();
            sum=sum+a;
            i++;
        }
        System.out.println(sum);
    }
}
//Using do while loop
import java.util.Scanner;
public class Funycode {
    public static void main(String[] args) {
        Scanner z = new Scanner(System.in);
        int i=1,a,sum=0;
        do
        {
            a = z.nextInt();
            sum=sum+a;
            i++;
        }while(i<=3);
        System.out.println(sum);
    }
}
```
### Output
```
input 3 4 5
output 12
```
