---
title: Write a C/C++/Java program to count total number of vowels and consonants in a string.
category: String Index
tags: ['string']
date: 2020-04-16
thumbnailText: Index
---

## C Implementation
### 13. Write a C program to count total number of vowels and consonants in a string.
```c
//Write a C program to count total number of vowels and consonants in a string.
#include<stdio.h>
#include <string.h>

int main()
{
    char str[1000];
    int i,vowels=0,consonants=0;

    printf("Enter  the string : ");
    gets(str);

    for(i=0;str[i];i++)
    {
        if((str[i]>=65 && str[i]<=90)|| (str[i]>=97 && str[i]<=122))
        {

            if(str[i]=='a'|| str[i]=='e'||str[i]=='i'||str[i]=='o'||str[i]=='u'||str[i]=='A'||str[i]=='E'||str[i]=='I'||str[i]=='O' ||str[i]=='U')
              vowels++;
            else
             consonants++;
        }

     }


    printf("vowels = %d\n",vowels);
    printf("consonants = %d\n",consonants);

    return 0;
}
```
### Output
```
Enter  the string : hello
vowels = 2
consonants = 3
```
## C++ Implementation
### 13. Write a C++ program to count total number of vowels and consonants in a string.
```c
//Write a C++ program to count total number of vowels and consonants in a string.

#include <iostream>
#include <string.h>
using namespace std;
int  main()
{
    char str[1000];
    int i,vowels=0,consonants=0;
    cout << "Enter  the string : ";
    gets(str);
    for(i=0;str[i];i++)
    {
        if((str[i]>=65 && str[i]<=90)|| (str[i]>=97 && str[i]<=122))
        {
            if(str[i]=='a'|| str[i]=='e'||str[i]=='i'||str[i]=='o'||str[i]=='u'||str[i]=='A'||str[i]=='E'||str[i]=='I'||str[i]=='O' ||str[i]=='U')
              vowels++;
            else
             consonants++;
        }
     }
    cout << "vowels = " << vowels << endl;
    cout << "consonants = " << consonants;
}
```
### Output
```
Enter  the string : hello
vowels = 2
consonants = 3
```

## Java Implementation
### 13. Write a Java program to count total number of vowels and consonants in a string.
```js
//Write a Java program to count total number of vowels and consonants in a string.
import java.util.Scanner;
public class Funycode {
    public static void main(String[] args) {
        Scanner z=new Scanner(System.in);
        System.out.println("Enter a string: ");
        String str, word = "";
        int i,vowels=0,consonants=0;
        str = z.nextLine();
        for(int i = str.length()-1; i >= 0; i --){
            if((str.charAt(i)>=65 && str.charAt(i)<=90)|| (str.charAt(i)>=97 && str.charAt(i)<=122)){

              if(str.charAt(i)=='a'|| str.charAt(i)=='e'||str.charAt(i)=='i'||str.charAt(i)=='o'||str.charAt(i)=='u'||str.charAt(i)=='A'||str.charAt(i)=='E'||str.charAt(i)=='I'||str.charAt(i)=='O' ||str.charAt(i)=='U')
                vowels++;
              else
               consonants++;
            }
        }
        System.out.println("vowels " + vowels);
        System.out.println("consonants = " + consonants);
    }
}
```
### Output
```
Enter  the string : hello
vowels = 2
consonants = 3
```
