---
title: Write a C/C++/Java program to find lower triangular matrix.
category: Array Program Index
tags: ['array']
date: 2020-04-16
thumbnailText: Index
---

## C Implementation
### 36. Write a C program to find lower triangular matrix.
```c
//Write a C program to find lower triangular matrix.
#include <stdio.h>
int main()
{
    int array1[3][3],i,j;
    printf("Enter 9 co-efficients of the matrix- \n");
    for (i = 0; i < 3; ++i)
    {
        for (j = 0; j < 3; ++j)
        {
            scanf("%d,", &array1[i][j]);
        }
    }
    printf("The given matrix is \n");
    for (i = 0; i < 3; ++i)
    {
        for (j = 0; j < 3; ++j)
            printf(" %d", array1[i][j]);
        printf("\n");
    }
    printf("\nSetting zero in lower triangular matrix\n");
    for(i=0; i<3; i++)
    {
        printf("\n");
        for(j=0; j<3; j++)
            if(i<=j)
                printf(" %d",array1[i][j]);
            else
                printf(" %d",0);
    }
}
```
### Output
```
Enter 9 co-efficients of the matrix-
1 2 3 4 5 6 7 8 9
The given matrix is
 1 2 3
 4 5 6
 7 8 9

Setting zero in lower triangular matrix

 1 2 3
 0 5 6
 0 0 9
```
## C++ Implementation
### 36. Write a C++ program to find lower triangular matrix.
```c
//Write a C++ program to find lower triangular matrix.

#include <iostream>
using namespace std;
int  main()
{
    int array1[3][3],i,j;
    cout << "Enter 9 co-efficients of the matrix- \n";
    for (i = 0; i < 3; ++i)
    {
        for (j = 0; j < 3; ++j)
        {
            cin >> array[i][j];
        }
    }
    cout << "The given matrix is \n";
    for (i = 0; i < 3; ++i)
    {
        for (j = 0; j < 3; ++j)
            cout << "\t" << arr1[i][j];
        cout << "\n";
    }
    cout << "\nSetting zero in lower triangular matrix\n";
    for(i=0; i<3; i++)
    {
        cout << "\n";
        for(j=0; j<3; j++)
            if(i<=j)
                cout << "\t" << arr1[i][j];
            else
                cout << " " << 0;
    }
}
```
### Output
```
Enter 9 co-efficients of the matrix-
1 2 3 4 5 6 7 8 9
The given matrix is
 1 2 3
 4 5 6
 7 8 9

Setting zero in lower triangular matrix

 1 2 3
 0 5 6
 0 0 9
```

## Java Implementation
### 36. Write a Java program to find lower triangular matrix.
```js
//Write a Java program to find lower triangular matrix.
import java.util.Scanner;
public class Funycode {
    public static void main(String[] args) {
          Scanner z=new Scanner(System.in);
          int array[][]= new int[3][3],i,j;
          System.out.printf("Enter 9 co-efficients of the matrix-\n");
          for (i = 0; i < 3; ++i)
          {
              for (j = 0; j < 3; ++j)
              {
                  array[i][j] = z.nextInt();
              }
          }
          System.out.printf("The given matrix is \n");
          for (i = 0; i < 3; ++i)
          {
              for (j = 0; j < 3; ++j)
                  System.out.printf("\t"+array[i][j]);
              System.out.printf("\n");
          }
          System.out.printf("\nSetting zero in lower triangular matrix\n");
          for(i=0; i<3; i++)
          {
              System.out.printf("\n");
              for(j=0; j<3; j++)
                  if(i<=j)
                      System.out.printf("\t"+array[i][j]);
                  else
                      System.out.printf(" "+0);
          }
      }
}
```
### Output
```
Enter 9 co-efficients of the matrix-
1 2 3 4 5 4 3 2 1
The given matrix is
 1 2 3
 4 5 4
 3 2 1
Sum of the 0 row is = 6
Sum of the 1 row is = 13
Sum of the 2 row is = 6
Sum of the 0 column is = 8
Sum of the 1 column is = 9
Sum of the 2 column is = 8
```
