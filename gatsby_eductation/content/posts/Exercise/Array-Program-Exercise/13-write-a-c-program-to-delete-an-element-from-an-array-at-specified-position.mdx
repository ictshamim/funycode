---
title: Write a C/C++/Java program to delete an element from an array at specified position.
category: Array Program Index
tags: ['array']
date: 2020-04-16
thumbnailText: Index
---

## C Implementation
### 13. Write a C program to delete an element from an array at specified position.
```c
//Write a C program to delete an element from an array at specified position.
#include <stdio.h>
void main(){
  int arr[10],i,pos;
  printf("Input 10 elements in the array:\n");
       for(i=0;i<10;i++)
        {
          scanf("%d",&arr[i]);
        }
  printf("\nInput the position where to delete: ");
  scanf("%d",&pos);
  i=0;
  while(i!=pos-1)
            i++;
  while(i<10){
            arr[i]=arr[i+1];
            i++;
  }
  n--;
   printf("\nThe new list is : ");
  for(i=0;i<10;i++)
        {
           printf("  %d",arr[i]);
        }
}
```
### Output
```
Input 10 elements in the array:
5
4
3
2
5
6
4
3
2
1
Input the position where to delete: 2
The new list is : 5 3 2 5 6 4 3 2 1 2
```
## C++ Implementation
### 13. Write a C++ program to delete an element from an array at specified position.
```c
//Write a C++ program to delete an element from an array at specified position.

#include<iostream>
using namespace std;
int main()
{
    int arr[10],i,pos,n;
    cout<<"Input 10 elements in the array:\n";
    for(i=0; i<10; i++)
    {
        cin>>arr[i];
    }
    cout<<"\nInput the position where to delete: ";
    cin>>pos;
    i=0;
    while(i!=pos-1)
        i++;
    while(i<10)
    {
        arr[i]=arr[i+1];
        i++;
    }
    n=n-1;
    cout<<"\nThe new list is : ";
    for(i=0; i<10; i++)
    {
        cout<<arr[i]<<"  ";
    }
}
```
### Output
```
Input 10 elements in the array:
5
4
3
2
5
6
4
3
2
1
Input the position where to delete: 2
The new list is : 5 3 2 5 6 4 3 2 1 2
```

## Java Implementation
### 13. Write a Java program to delete an element from an array at specified position.
```js
//Write a Java program to delete an element from an array at specified position.
import java.util.Scanner;
public class Funycode {
    public static void main(String[] args) {

        Scanner z=new Scanner(System.in);
            int arr[]=new int[10],i,pos,n=0;
            System.out.println("Input 10 elements in the array:\n");
            for(i=0; i<10; i++)
            {
                arr[i]=z.nextInt();
            }
            System.out.println("\nInput the position where to delete: ");
            pos=z.nextInt();
            i=0;
            while(i!=pos-1)
                i++;
            while(i<10)
            {
                arr[i]=arr[i+1];
                i++;
            }
         System.out.println("\nThe new list is : ");
            for(i=0; i<10; i++)
            {
                System.out.println(arr[i]+"  ");
            }
    }
}
```
### Output
```
Input 10 elements in the array:
5
4
3
2
5
6
4
3
2
1
Input the position where to delete: 2
The new list is : 5 3 2 5 6 4 3 2 1 2
```
