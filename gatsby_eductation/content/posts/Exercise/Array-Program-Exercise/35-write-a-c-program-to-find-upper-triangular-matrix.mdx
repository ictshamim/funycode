---
title: Write a C/C++/Java program to find upper triangular matrix.
category: Array Program Index
tags: ['array']
date: 2020-04-16
thumbnailText: Index
---

## C Implementation
### 35. Write a C program to find upper triangular matrix.
```c
//Write a C program to find upper triangular matrix.
#include <stdio.h>
int main()
{
    int array1[3][3],i,j;
    printf("Enter 9 co-efficients of the matrix- \n");
    for (i = 0; i < 3; ++i)
    {
        for (j = 0; j < 3; ++j)
        {
            scanf("%d,", &array1[i][j]);
        }
    }
    printf("The given matrix is \n");
    for (i = 0; i < 3; ++i)
    {
        for (j = 0; j < 3; ++j)
            printf(" %d", array1[i][j]);
        printf("\n");
    }
    printf("\nSetting zero in upper triangular matrix\n");
    for(i=0; i<3; i++)
    {
        printf("\n");
        for(j=0; j<3; j++)
            if(i>=j)
                printf(" %d",array1[i][j]);
            else
                printf(" %d",0);
    }
}
```
### Output
```
Enter 9 co-efficients of the matrix-
1 2 3 4 5 6 7 8 9
The given matrix is
 1 2 3
 4 5 6
 7 8 9

Setting zero in upper triangular matrix

 1 0 0
 4 5 0
 7 8 9
```
## C++ Implementation
### 35. Write a C++ program to find upper triangular matrix.
```c
//Write a C++ program to find upper triangular matrix.

#include <iostream>
using namespace std;
int  main()
{
    int array1[3][3],i,j;
    cout << "Enter 9 co-efficients of the matrix-\n";
    for (i = 0; i < 3; ++i)
    {
        for (j = 0; j < 3; ++j)
        {
            cin >> array[i][j];
        }
    }
    cout << "The given matrix is \n";
    for (i = 0; i < 3; ++i)
    {
        for (j = 0; j < 3; ++j)
            cout << "\t" << arr1[i][j];
        cout << "\n";
    }
    printf("\nSetting zero in upper triangular matrix\n");
    for(i=0; i<3; i++)
    {
        cout << "\n";
        for(j=0; j<3; j++)
            if(i>=j)
                cout << "\t" << arr1[i][j];
            else
                cout << " " << 0;
    }
}
```
### Output
```
Enter 9 co-efficients of the matrix-
1 2 3 4 5 6 7 8 9
The given matrix is
 1 2 3
 4 5 6
 7 8 9

Setting zero in upper triangular matrix

 1 0 0
 4 5 0
 7 8 9
```

## Java Implementation
### 35. Write a Java program to find upper triangular matrix.
```js
//Write a Java program to find upper triangular matrix.
import java.util.Scanner;
public class Funycode {
    public static void main(String[] args) {
          Scanner z=new Scanner(System.in);
          int array1[][] = = new int[3][3],i,j;
          System.out.printf("Enter 9 co-efficients of the matrix- \n");
          for (i = 0; i < 3; ++i)
          {
              for (j = 0; j < 3; ++j)
              {
                  scanf("%d,", &array1[i][j]);
              }
          }
          printf("The given matrix is \n");
          for (i = 0; i < 3; ++i)
          {
              for (j = 0; j < 3; ++j)
                  System.out.printf("\t"+array[i][j]);
              System.out.printf("\n");
          }
          System.out.printf("\nSetting zero in upper triangular matrix\n");
          for(i=0; i<3; i++)
          {
              System.out.printf("\n");
              for(j=0; j<3; j++)
                  if(i>=j)
                      System.out.printf("\t"+array[i][j]);
                  else
                      System.out.printf(" " + 0);
          }
      }
}
```
### Output
```
Enter 9 co-efficients of the matrix-
1 2 3 4 5 6 7 8 9
The given matrix is
 1 2 3
 4 5 6
 7 8 9

Setting zero in upper triangular matrix

 1 0 0
 4 5 0
 7 8 9
```
