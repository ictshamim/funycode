---
title: write a assembly language program to a. prompt the user b. Read first middle and last initials of a person’s name c. And display them down the left margin
slug: write-a-program-to-a-prompt-the-user-b-read-first-middle-and-last-initials-of-a-persons-name
category: Assembly Language Index
tags: ['Assembly Language']
date: 2020-04-30
thumbnailText: Assembly Language
---
### 13. write a program to a. prompt the user b. Read first middle and last initials of a person’s name c. And display them down the left margin
```
.model small
.stack 100h
.data
    str db 'enter thrre initials: $'
    str1 db '',0ah,0dh
    first db ?
    str2 db '',0ah,0dh
    second db ?
    str3 db '',0ah,0dh
    third db ?
    str4 db '$'
.code
main proc

    mov ax,@data
    mov ds,ax

    mov ah,9
    lea dx,str
    int 21h

    mov ah,1
    int 21h
    mov first,al
    int 21h
    mov second,al
    int 21h
    mov third,al

    mov ah,9
    lea dx,str1
    int 21h

    mov ah,4ch
    int 21h

    main endp
end main

```

## Input
```
abc
```
## Output
```
a
b
c
```