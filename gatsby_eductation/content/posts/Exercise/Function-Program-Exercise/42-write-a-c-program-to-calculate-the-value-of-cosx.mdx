---
title: Write a C/C++/Java program to find the square of any number using the function.
category: Function Program Index
tags: ['function']
date: 2020-04-16
thumbnailText: Index
---

## C Implementation
### 42. Write a C program to Calculate the Value of cos(x)
```c
//Write a C program to Calculate the Value of cos(x)
#include<stdio.h>

float Cosx(float x,int n)
{
    int fact,sign=-1,i,j;
    float p,sum=0.0;
    for (i = 2; i <= n; i += 2)
    {
        p = 1;
        fact = 1;
        for (j = 1; j <= i; j++)
        {
            p = p * x;
            fact = fact * j;
        }

        sum += sign * p / fact;
        sign =  - 1 * sign;
    }
    return sum;
}

int main()
{
    float x;
    int n;
    printf("Enter value of x: ");
    scanf("%f",&x);
    printf("Enter value of n: ");
    scanf("%d",&n);

    float val=Cosx(x,n);

    printf("cos(%0.2f) = %.2f", x, 1+val);
    return 0;
}
```
### Output
```
Enter value of x: 3
Enter value of n: 4
Cos(3.0) = -1.125
```

## C++ Implementation
### 42. Write a C++ program to Calculate the Value of cos(x)
```c
//Write a C++ program to Calculate the Value of cos(x)

#include<iostream>
using namespace std;

float Cosx(float x,int n)
{
    int fact,sign=-1,i,j;
    float p,sum=0.0;
    for (i = 2; i <= n; i += 2)
    {
        p = 1;
        fact = 1;
        for (j = 1; j <= i; j++)
        {
            p = p * x;
            fact = fact * j;
        }

        sum += sign * p / fact;
        sign =  - 1 * sign;
    }
    return sum;
}

int main()
{
    float x;
    int n;
    cout << "Enter value of x: ";
    cin >> x;
    cout << "Enter value of n: ";
    cin >> n;

    float val=Cosx(x,n);

    cout << "cos(" << x << ") = "<< val;
    return 0;
}
```
### Output
```
Enter value of x: 3
Enter value of n: 4
Cos(3.0) = -1.125
```

## Java Implementation
### 42. Write a Java program to Calculate the Value of cos(x)
```js
//Write a Java program to Calculate the Value of cos(x)
import java.util.Scanner;
public class Funycode {
    static float Cosx(float x,int n)
    {
        int fact,sign=-1,i,j;
        float p,sum=0;
        for (i = 2; i <= n; i += 2)
        {
            p = 1;
            fact = 1;
            for (j = 1; j <= i; j++)
            {
                p = p * x;
                fact = fact * j;
            }
            sum += sign * p / fact;
            sign =  - 1 * sign;
        }
        return sum;
    }

    public static void main(String[] args) {
        Scanner z = new Scanner(System.in);
        float x;
        int n;
        System.out.println("Enter value of x: ");
        x = z.nextFloat();
        System.out.println("Enter value of n: ");
        n = z.nextInt();
        float val=Cosx(x,n);
        System.out.println("Cos(" + x + ") = "+ val);
    }
}
```
### Output
```
Enter value of x: 3
Enter value of n: 4
Cos(3.0) = -1.125
```
